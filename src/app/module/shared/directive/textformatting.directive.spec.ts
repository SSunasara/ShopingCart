import { TextformattingDirective } from './textformatting.directive';

describe('TextformattingDirective', () => {
  it('should create an instance', () => {
    const directive = new TextformattingDirective();
    expect(directive).toBeTruthy();
  });
});
